# -----------------------------------------------------------------------
# Local development only (npm run dev)
# Create a file called .env.local in frontend/ and set this value.
# When running via docker compose, API_URL is injected as a build arg —
# no .env file is needed.
# -----------------------------------------------------------------------

API_URL=http://localhost:8000
